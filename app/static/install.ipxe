#!ipxe
set base-uri https://pxe.physik-pool.tu-berlin.de/debian-install
kernel ${base-uri}/linux initrd=initrd.gz initrd=preseed.cfg.cpio
initrd ${base-uri}/initrd.gz
initrd ${base-uri}/preseed.cfg.cpio
boot
